# Kranz 0.9.0 social copy (draft)

> Draft only; publish after a tagged release and recorded client smoke test.

Your coding agent joins the Kranz session you already run.

It sees the same services, readiness, ports, actions, run numbers, and bounded
logs as the TUI—then it can restart a service and wait for readiness without
starting a second dev stack. Disconnect the attached agent; keep the stack.
